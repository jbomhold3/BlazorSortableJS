@inherits DynamicComponentBase
@typeparam TItem
@if (Item != null)
{
    <DynamicElement Attributes="@UnknownParameters" TagName="@Tag" class="@Class" @onclick="@OnClick" Id="@Id" data-id="@Item.DataId">
        @ChildContent
    </DynamicElement>
}
@code {
    [Parameter] public string Id { get; set; }
    [Parameter] public string Class { get; set; }
    [Parameter] public string Style { get; set; }
    [Parameter] public bool IsDiv { get; set; }
    [Parameter] public RenderFragment ChildContent { get; set; }
    [Parameter] public EventCallback<UIMouseEventArgs> OnClick { get; set; }
    [Parameter] public SortableJSSortItem<TItem> Item { get; set; }
    [CascadingParameter] public SortableJS<TItem> Sortable { get; set; }


    string Tag
    {
        get
        {
            return (IsDiv) ? "div" : "li";
        }
    }

    protected override Task OnAfterRenderAsync()
    {
        return base.OnAfterRenderAsync();
    }
}